import java.util.*

class `1003_SimpleSum` {

    fun main(args: Array<String>) {
        val leitor = Scanner(System.`in`)
        val termoUm = leitor.nextInt()
        val termoDois = leitor.nextInt()

        println("SOMA = ${termoUm + termoDois}")

    }
}