import java.util.*

class `1016_Distance` {

    fun main(args: Array<String>) {
        val leitor = Scanner(System.`in`)
        val speed = leitor.nextInt()

        println("${speed * 2} minutos")
    }
}
